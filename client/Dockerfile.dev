FROM node:18-alpine

WORKDIR /app

# Install Python and build dependencies for node-gyp (if needed)
RUN apk add --no-cache python3 make g++ gcc

# Copy package.json files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy project files
COPY . .

# Expose port
EXPOSE 3000

# Start the application in development mode
CMD ["npm", "start"]